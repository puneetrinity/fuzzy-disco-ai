[phases.setup]
nixPkgs = ['nodejs-18_x', 'npm-8_x']

[phases.install]
cmds = [
  'npm install --no-package-lock',
  'npm run build'
]

[phases.build]
cmds = ['echo "Build completed in install phase"']

[start]
cmd = 'npm start'